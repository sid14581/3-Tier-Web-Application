apiVersion: v1
kind: Service 
metadata:
  namespace: three-tier
  name: mongo-svc
spec:
  selector:
    app: mongodb
  
  ports:
  - port: 27017
    targetPort: 27017
    protocol: TCP
    name: mongo-svc