apiVersion: storage.k8s.io/v1
kind: CSIDriver
metadata:
  name: efs.csi.aws.com
  namespace: three-tier
spec:
  attachRequired: false

---

apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: efs-sc
  namespace: three-tier
provisioner: efs.csi.aws.com
parameters:
  fileSystemId: fs-0a14425b88f5d6bd1 
reclaimPolicy: Delete
volumeBindingMode: Immediate
